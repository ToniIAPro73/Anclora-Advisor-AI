# Agent D - QA

Validar:

1. Persistencia segura en `invoices`.
2. Calculo total consistente entre cliente y servidor.
3. Listado de facturas por usuario.
4. Estados de error y vacio.

Checks:

- `npm run -s lint`
- `npm run -s type-check`
- `npm run -s build`

